// Code generated by pkg/widget/internal/uigen from the NodeStatus widget document. DO NOT EDIT.

package nodestatus

import "github.com/candacelabs/candace/pkg/gotth/live"

// NodeStatusView draws the NodeStatus widget's live region.
//
// Every value in it is a pure read of state, because the same state must render
// byte-identical markup: that comparison is what suppresses a patch nobody needs.
templ NodeStatusView(state NodeStatusState) {
	<aside { live.Region(NodeStatusRegion)... } class="widget" aria-labelledby={ NodeStatusTitleID } data-widget="NodeStatus" data-palette="fieldStation">
		<header class="widget-chrome">
			<h2 class="widget-title widget-token-ink" id={ NodeStatusTitleID }>{ state.TitleLabel() }</h2>
		</header>
		<svg class="widget-scene" viewBox="0 0 100 100" preserveAspectRatio="xMidYMid meet" role="img" aria-label={ state.SceneDescriptionLabel() }>
			<g class="widget-node widget-role-node" transform="translate(50,50)">
				<circle class="widget-marker widget-marker-large widget-token-accent" r="9"></circle>
				<text class="widget-node-title widget-token-ink" y="-14" text-anchor="middle">{ state.NodeALabel() }</text>
				<text class="widget-node-caption widget-token-muted" y="20" text-anchor="middle">{ state.StatusLabel() }</text>
			</g>
		</svg>
	</aside>
}
